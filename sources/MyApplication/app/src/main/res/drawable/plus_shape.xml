<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <size
        android:width="100dp"
        android:height="100dp" />
    <solid android:color="@android:color/holo_blue_light" /> <!-- Kolor tła plusa -->

    <!-- Pionowa linia plusa -->
    <corners android:radius="4dp" /> <!-- Zaokrąglenie krawędzi -->
    <size
        android:width="60dp"
        android:height="10dp" />

    <!-- Pozioma linia plusa -->
    <corners android:radius="4dp" /> <!-- Zaokrąglenie krawędzi -->
    <size
        android:width="10dp"
        android:height="60dp" />
</shape>